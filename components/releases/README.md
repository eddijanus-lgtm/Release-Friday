# Release components

This folder contains reusable presentation components for music releases.

Planned components:

- `ReleaseCard`
- `ReleaseList`
- `ReleaseFilters`
- `ReleaseDateHeader`
- `StreamingLinks`

Keep data loading in `app/` or `lib/`. Components in this folder should receive typed props from `types/release.ts`.
